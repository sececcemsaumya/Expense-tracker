/*const http = require('http');
const server = http.createServer((req,res)=>{
    res.writeHead(200,{"Content-Type":"text/plain"});
    res.write("Calaculator\n");
    const sum = modules.add(10,20);
    res.write(`Addition : ${sum}\n`);
    const diff = modules.subtract(10,5);
    res.write(`Difference : ${diff}\n`);
    const multiplication  = modules.multiply(10,5);
    res.write(`Multiplication : ${multiplication}\n`);
    const quotient= modules.divide(10,5);
    res.write(`Quotient : ${quotient}\n`);
    const remainder = modules.remainder(10,5);
    res.write(`remainder : ${remainder}`);
    res.end();
});
server.listen(3000,()=>{
    console.log("server is running on port 3000")
})

const modules = require('./modules')*/





